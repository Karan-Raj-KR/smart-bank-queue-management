[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "alembic upgrade head && python ml/train.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
